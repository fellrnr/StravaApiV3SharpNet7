using System.Runtime.Serialization;

namespace de.schumacher_bw.Strava.Model
{
    /// <summary>An enumeration of the activity types ride run and walk.</summary>
    public enum ActivityTypeRideRunWalk
    {
        Ride = 1,
        Run = 2,
        Walk = 3,
    }
}

